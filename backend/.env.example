# 本地开发环境变量示例
# 复制此文件为 .env 并填写实际值

# 应用端口（可选，默认 3000）
PORT=3000

# GitHub Personal Access Token
# 获取方式：GitHub Settings > Developer settings > Personal access tokens > Tokens (classic)
# 所需权限：read:user（读取用户信息）
# 注意：Phase 1-7 期间这个功能无法使用（需要 NAT Gateway）
GITHUB_TOKEN=ghp_your_github_token_here

# 数据库连接字符串
# Phase 1: 使用内存存储，暂不需要
# Phase 2: 本地 PostgreSQL（⚠️ 现在需要配置）
# Phase 7: 将配置 Aurora Serverless v2

# 本地 PostgreSQL 示例
# 格式：postgresql://用户名:密码@主机:端口/数据库名?schema=public
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/nest_aws_dev?schema=public"

# 如果使用 Docker 启动 PostgreSQL：
# docker run --name postgres-nest-aws -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=nest_aws_dev -p 5432:5432 -d postgres:16
